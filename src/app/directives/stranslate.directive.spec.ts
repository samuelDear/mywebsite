import { StranslateDirective } from './stranslate.directive';

describe('StranslateDirective', () => {
  it('should create an instance', () => {
    const directive = new StranslateDirective();
    expect(directive).toBeTruthy();
  });
});
